[project]
name = "academic-workstation"
version = "0.2.0"
description = "Evidence-first orchestration for research and Office artifact validation."
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
